# On Omarchy 1Password is preinstalled; only the SSH-key setup runs there.
# (The apt install script that used to run on the Debian laptop was removed;
# containers skip the whole recipe via recipes/.recipeignore.)